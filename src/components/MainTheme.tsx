import { createTheme } from '@mui/material/styles';

const MainTheme = createTheme({
  palette: {
    background: {
      paper: '#fff',
      lightGray: '#f0f3f5',
      gray: '#919497'
    } as any,
  },
});

export default MainTheme;
